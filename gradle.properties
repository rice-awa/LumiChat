# Done to increase the memory available to gradle.
org.gradle.jvmargs=-Xmx6G
org.gradle.parallel=true

# 启用 toolchain 自动下载：CI 单 job buildAndCollect 仅装一个基础 JDK，
# Gradle 按 build.gradle.kts 的 toolchain 配置自动拉取其余所需 JDK。
org.gradle.java.installations.auto-download=true

# Stonecutter Mod Properties
mod.id=lumichat
mod.name=LumiChat
mod.version=2.1.0
mod.group=com.riceawa

# Dependencies
deps.fabric_loader=0.18.4